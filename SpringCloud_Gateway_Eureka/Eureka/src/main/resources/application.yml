spring:
  application:
    name: eureka_server

server:
  port: 8081

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8081/eureka/
